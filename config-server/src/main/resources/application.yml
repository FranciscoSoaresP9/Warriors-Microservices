server:
  port: 8095

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FranciscoSoaresP9/warriorsConfig
          clone-on-start: true
